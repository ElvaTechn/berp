// Simple placeholder icons for PWA
// In production, replace with proper icon generation

const generateIcon = (size: number) => {
  // Simple blue square with white text
  const svg = `
    <svg width="${size}" height="${size}" viewBox="0 0 ${size} ${size}" fill="none" xmlns="http://www.w3.org/2000/svg">
      <rect width="${size}" height="${size}" rx="${size * 0.2}" fill="#2563eb"/>
      <text x="50%" y="50%" text-anchor="middle" dy=".3em" fill="white" font-size="${size * 0.4}" font-family="Arial, sans-serif" font-weight="bold">S</text>
    </svg>
  `;
  return svg;
};

// Generate all icon sizes
const sizes = [72, 96, 128, 144, 152, 192, 384, 512];

sizes.forEach(size => {
  const svg = generateIcon(size);
  console.log(`<!-- ${size}x${size} -->`);
  console.log(svg);
});