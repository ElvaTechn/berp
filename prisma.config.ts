/**
 * ================================================================
 * PRISMA CONFIGURATION - BizControl 360 ERP
 * ================================================================
 * Configuração para Prisma 7+
 * Nota: Seed não é mais configurado aqui no Prisma 6.x
 * Use package.json para seed commands
 * ================================================================
 */

import { defineConfig } from 'prisma/config';

export default defineConfig({
  // Configurações futuras do Prisma 7+ virão aqui
  // Por enquanto, mantemos vazio pois seed é gerenciado via package.json
});
